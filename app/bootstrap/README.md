# Bootstrap / контейнер зависимостей

Кратко: сборка всех клиентов, use case-ов и фоновых интеграций (Telegram polling, scheduler) для запуска FastAPI-приложения.

## Локальный запуск

Сервер поднимается из корня репозитория (см. основной README проекта), не из этой папки.

## Тесты

Из корня репозитория:

```bash
PYTHONPATH=. pytest
```

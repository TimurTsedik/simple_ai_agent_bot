# Defaults for tool `digest_telegram_news` / skill `telegram_news_digest` when the user omits channels/keywords.
# User-topic digests («по теме …») use long-term memory + `user_topic_telegram_digest`; they do NOT read these lists.
telegramNewsDigest:
  digestChannelUsernames: []
  portfolioTickers: []
  digestSemanticKeywords: []

emailReader:
  accountName: "gmail"
  email: "your.email@example.com"
  imapHost: "imap.gmail.com"
  imapPort: 993
  imapSsl: true
  smtpHost: "smtp.gmail.com"
  smtpPort: 465
  smtpSsl: true

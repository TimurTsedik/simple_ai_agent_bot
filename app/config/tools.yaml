# Опциональные дефолты только для `digest_telegram_news` (общий дайджест без «по теме»).
# Дайджесты по теме — см. skill `user_topic_telegram_digest` и память digest_topic_config_json.
telegramNewsDigest:
  digestChannelUsernames: []
  portfolioTickers: []
  digestSemanticKeywords: []

emailReader:
  accountName: "gmail"
  email: "timur.tsedik@gmail.com"
  imapHost: "imap.gmail.com"
  imapPort: 993
  imapSsl: true
  smtpHost: "smtp.gmail.com"
  smtpPort: 465
  smtpSsl: true
